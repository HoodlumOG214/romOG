id: mariocube
name: MarioCube
homepage: https://archive.mariocube.com
kind: catalog
priority: 70
auth:
  required: false
capabilities:
  - http_range_resume
  - filename_listing
platforms:
  - dsi
  - wii
notes: |
  Wii / DSi mirror. Returns a plain-text directory listing to curl-style
  clients (User-Agent: curl/8.0). Uses ANSI-colored output we strip
  during parsing.
