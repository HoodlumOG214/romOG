"""
Source plugin packages.

Each subpackage under this directory is a single source plugin. The
build pipeline auto-discovers them via db/core/registry.py — adding a
new source means dropping a new folder here, no edits elsewhere.
"""
